#include "main.h"
/**
 * _islower -  checks for lowercase character
 *@c: is the character to be checked
 * Return: 1m if the character is lowercase, otherwisw 0
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z'0
			return (1);
	else
			return (0);
}
