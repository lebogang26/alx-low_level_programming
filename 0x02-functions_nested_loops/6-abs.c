#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @n: the intiger in the question
 * Retur: the abs of the integer
 */
int_abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
